{
  "name": "@f/is-dom-loaded",
  "description": "Check whether or not the document has finished loading",
  "repository": "git://github.com/micro-js/is-dom-loaded.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
